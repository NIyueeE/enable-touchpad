[package]
name = "etp-ffi"
version = "0.1.0"
edition = "2024"
description = "Minimal Win32 FFI for enable-touchpad: precision-touchpad state queries"
license = "MIT OR Apache-2.0"

[lints.rust]
missing_docs = "warn"
unsafe_code = "deny"
